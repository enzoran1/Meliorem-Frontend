import React, { Fragment } from "react";

const QuizTitleProgress = (props) => <Fragment>{props.title}</Fragment>;

QuizTitleProgress.propTypes = {};

QuizTitleProgress.defaultProps = {};

export default QuizTitleProgress;

import React from 'react';


const ClassementBadges = (props) => (
  <img src={props.image} alt="" />
);

ClassementBadges.propTypes = {};

ClassementBadges.defaultProps = {};

export default ClassementBadges;
